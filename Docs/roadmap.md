# Roadmap и журнал работ

Цель первой версии — см. `Docs/product.md`. Архитектура — `Docs/intelligence.md`.
Статусы: ☐ не начато · ◐ в работе · ☑ сделано · ⚠ требует проверки на Mac.

## Этап 0. Инфраструктура (Linux) — ☑

- ☑ Бриф заказчика сохранён в `Docs/brief.md`.
- ☑ `Package.swift` (path `Linea/Core`, паритет настроек Xcode), `Tests/`,
  `Scripts/test-core.sh` (Docker, swift:6.3), `Scripts/check-layers.sh`,
  CI `core-linux.yml` (блокирующий) и `ios-macos.yml` (вручную).
- ☑ Перенос существующих моделей и протоколов в `Linea/Core`, реализаций —
  в `Linea/Data` (внутри synchronized group, pbxproj не тронут).

## Этап 1. Доменная модель ядра (Linux) — ☑

- ☑ `TimeContext`, `Signal`/`SignalKind`/`ProviderID`, `Commitment`,
  `ContextSnapshot`, `DailyHealthSummary`/`Baseline`/`SleepNight`,
  `UserProfile`, `NutritionProfile`/`MealLog`, `UserState`/`Fact`,
  `DayPlan`/`PlanBlock`/`Recommendation`, `Nudge`, `UserFeedback`,
  `Calibration`/`EngineConfig`, `DayRecord`.
- ☑ `LineaTask` += deadline, scheduledStart, estimatedMinutes,
  cognitiveDemand, goalID, completedAt; `LineaGoal` += horizon, startDate,
  isActive. Аддитивно, существующий UI компилируется без изменений.
- ☑ Протоколы: `ContextProvider`, `StateAnalyzer`, `PlanRule`, `NudgeRule`,
  `Explainer`/`TextRenderer`, репозитории, `HealthHistorySource`.
- ☑ `WowFixture` (сценарий брифа в числах), фейки, Codable round-trip,
  `Circadian`.

## Этап 2. Движки (Linux) — ☑

- ☑ State Engine: SleepAnalyzer (дедупликация источников), BaselineCalculator
  (медиана/MAD, полы, окно 28 дней), анализаторы sleep / recovery / strain,
  EnergyFusion с гистерезисом; покрыт тестами (55 тестов ядра зелёные).
- ☑ Аналитика сна (`SleepInsight`) для экрана Health.
- ☑ Подсказка связи задачи с целью (`KeywordGoalMatcher`).
- ☑ Decision Engine: TaskScorer, FreeWindows, DayPlanner, правила
  (DayBrief, LoadAdjustment), NudgeEngine (BehindSchedule, EveningCheckIn),
  пересборка плана.
- ☑ Объяснения: RussianText, RuleBasedExplainer (golden-строки брифа слово в
  слово), ExplanationValidator, FallbackExplainer.
- ☑ ContextEngine + CommitmentMapper, FeedbackEngine, коннектор Nutrition
  (провайдер окон еды, правило обеда и перекуса, компонент «топливо»).
- ☑ Use cases: PlanDay, AcceptPlan, CheckIn, RespondToNudge, RecordDayRating;
  сквозной тест wow-сценария (утро → 14:30 → вечер) и деградации без данных.
- ☑ 162 теста ядра зелёные в Docker, включая первый запуск без данных,
  пустой день, поздний вечер, просроченные задачи и смену часового пояса.

## Этап 3. Apple-слой (пишется на Linux, компилируется на Mac) — ◐ ⚠

- ☑ `TaskEntity`/`GoalEntity`: новые optional-поля (lightweight-миграция);
  `DayRecordEntity`, `DocumentEntity` (калибровка, профиль, питание),
  `MealLogEntity`; `Local*Repository`; общая схема `LineaSchema`.
- ☑ `HealthKitHistoryReader` (28 дней: сегменты сна с источниками, HRV,
  RHR, шаги, энергия, тренировки) и `HealthKitContextProvider`;
  тайл сна в `HealthKitManager` переведён на `SleepAnalyzer`.
- ☑ `AppContainer` (репозитории, хранилища, провайдеры, `TimeContext.live`).
- ☑ `IntelligenceStore` (состояние, план, нуджи, калибровка, ответы AI-экрана)
  и подписка на изменения задач, целей, профиля и питания.
- ☑ Редакторы: задача (дедлайн, время начала, длительность, сложность,
  цель + подсказка), цель (горизонт, активность), питание (профиль,
  окна еды, «Поел»), «О себе» (рабочий день, тихие часы, сон).
- ☑ Today: утренний бриф, «Принять план», тайм-блоки, карточка нуджа,
  вечерняя оценка, советы по питанию. Health: «Сон за 7 дней» с графиком
  против личной нормы. Nutrition: профиль и «Поел». Profile: «Подключения»,
  «AI», «Калибровка», «О себе». Демо-данные удалены.
- ☑ `Platform/NudgeScheduler` (UserNotifications с категориями действий) и
  `AppDelegate` для ответов на уведомления.
- ☐ `Platform/FoundationModelsExplainer` (on-device модель iOS 26) — **не
  начат осознанно**: API нельзя проверить без Mac, а ошибка в нём уронит
  сборку целиком. Точка подключения готова:
  `FallbackExplainer(primary:)` в `AppContainer`; сегодня передаётся `nil`,
  и приложение работает на шаблонах.

## Этап 4. Сборка на macOS-раннере — ☑

Выбранный путь: GitHub Actions, воркфлоу `.github/workflows/ios-macos.yml`.
Он запускается на push в `main`, на пул-реквестах и вручную; при провале
кладёт список ошибок компиляции в summary и полный лог в артефакты, чтобы
их можно было чинить без Mac.

Подготовлено:
- ☑ общая схема `Linea.xcodeproj/xcshareddata/xcschemes/Linea.xcscheme`
  (без неё `xcodebuild -scheme Linea` на CI падает: локальная схема лежит в
  `xcuserdata` и не коммитится);
- ☑ шаг диагностики: печатает доступные Xcode и выбирает 26-й, иначе самый
  новый с предупреждением;
- ☑ `swift test` на Apple-тулчейне — ловит расхождения Foundation.

**Готово.** Ветка `feature/ios-intelligence-core` собирается на GitHub
Actions: `** BUILD SUCCEEDED **`, предупреждений в коде Linea нет, и те же
162 теста ядра проходят ещё раз на Apple-тулчейне (Xcode 26.6).

Что чинилось по дороге:
1. **Изоляция репозиториев.** Протоколы были `nonisolated ... : Sendable`,
   из-за чего их требования становились неизолированными и реализации на
   `@MainActor` теряли доступ к собственному `ModelContext`. Протоколы
   репозиториев теперь привязаны к главному актору — там, где и живут их
   реализации на SwiftData. `ContextProvider` и `HealthHistorySource`
   остались `nonisolated Sendable`: они действительно неизолированные.
2. **Имена тренировок.** `HKWorkoutActivityType.displayName` читался из
   неизолированного контекста; помечен `nonisolated` (иначе ошибка в
   языковом режиме Swift 6).
3. **Лишний `nonisolated(unsafe)`** на `HKHealthStore`, который и так
   `Sendable`.

Раннер: метка `macos-26` не существует, задание идёт на `macos-15` с
Xcode 26.6 — шаг «Toolchain» сам выбирает самый новый Xcode и пишет об этом
предупреждение.

## Этап 5. Проверка на устройстве и приёмка — ☐ ⚠

Чек-лист (выполняет тот, у кого есть iPhone с данными):
1. `xcodebuild -project Linea.xcodeproj -scheme Linea -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO` — без ошибок.
2. `swift test` на macOS-тулчейне (Apple Foundation vs swift-corelibs).
3. Старая сборка с данными → новая: lightweight-миграция прошла, задачи/цели на месте.
4. HealthKit: тайлы совпадают с Health.app; «Сон» = ночь без двойного счёта; история 28 дней и baseline; отказ доступа → честный текст.
5. Утренний план на реальных данных: тексты, три действия, «Принять план», блоки без пересечений.
6. Нудж: задача не закрыта → карточка и уведомление с действиями; закрыть задачу → уведомление снято.
7. Вечерняя оценка → Profile «Калибровка» показывает изменения.
8. Уведомления: разрешение запрашивается при первом «Принять план», кнопки «Закрываем сейчас» / «Переносим» доходят до приложения, выполненная задача снимает уведомление.
9. Xcode-навигатор: `Linea/Core`, `Data`, `Platform` видны; `Tests/` и `Package.swift` не в таргете.
10. Previews (`PlanStore.preview`, `RootView`) компилируются.
11. Экран «Питание»: редактор профиля, чипы, «Поел» — и обед появляется в плане дня.
12. Экран «Профиль» → «Калибровка»: после вечерней оценки значения меняются.

## Этап 6. Календарь — ◐ ⚠

Второй коннектор, сделан по рецепту `Docs/connectors.md` без единой правки
движков:
- ☑ `Linea/Core/Connectors/Calendar` — какие события занимают время
  (не весь день, не «свободен», не отменённые, не короче 10 минут и не
  длиннее 12 часов), покрыто тестами в Docker;
- ☑ `Linea/Data/Providers/Calendar` — тонкий адаптер EventKit;
- ☑ переключатель в «Профиле» → «Подключения»: разрешение спрашивается по
  нажатию, а не посреди пересчёта плана;
- ☑ ключ `NSCalendarsFullAccessUsageDescription` в настройках таргета;
- ☐ проверка на устройстве: события из настоящего календаря должны
  вычитаться из свободного времени, а нудж — считать «до следующего
  обязательства» по ним.

Заодно закрыта скрытая проблема: сохранённые JSON-документы (профиль,
питание, калибровка) теперь читаются мягко. Раньше добавление любого поля
уронило бы чтение старой записи и молча сбросило настройки и всю калибровку.

## Этап 7. Правки после созвона с заказчиком (16.09.2026) — ◐ ⚠

Ветка `feature/customer-feedback-round-1`, сборка на CI зелёная.

1. ☑ **Горизонт цели → срок.** Вместо «неделя/месяц» у цели явная дата
   окончания, необязательная. Чем ближе срок, тем выше Decision Engine
   поднимает связанные задачи; просроченная цель даёт максимум. Цели,
   созданные раньше, получают срок из старого горизонта — данные не теряются.
2. ☑ **Приоритет вместо важности.** Три уровня: низкий, средний, высокий.
   Сохранённые задачи читаются как раньше: поменялись только подписи.
3. ☑ **Здоровье не подтягивало данные.** Нашлись две причины. Показатели
   читались один раз за сессию и больше не обновлялись. И как только приходил
   хотя бы один показатель, кнопка настройки доступа пропадала — включить
   остальные категории было негде. Теперь экран перечисляет, чего именно не
   видит, ведёт в приложение «Здоровье» и умеет обновляться жестом.
4. ☑ **Чат подключён к модели.** Клиент провайдер-независимый: адрес, модель и
   путь — настройки, по умолчанию Grok от xAI. Свободный вопрос уходит модели
   вместе с кратким контекстом дня, три готовые подсказки отвечают мгновенно
   и без сети. Выключено, пока пользователь не включит в «Профиле».
   Ключ — `Docs/secrets.md`.
5. ☑ **ВкусВилл.** Официальный MCP-эндпоинт подключён: поиск товаров и
   сборка ссылки на корзину из подходящих продуктов. Главное ограничение —
   истории заказов в их API нет, поэтому учёт питания возможен только когда
   корзину собирает само приложение (сделано) либо по штрихкоду (следующий
   шаг). Подробности и проверенные факты — `Docs/vkusvill.md`.
6. ☑ **Одна кнопка настроек питания** вместо шести строк; заодно поправлен
   текст, который переносился внахлёст.
7. ☑ **Календарь** влит в `main` (заказчик тянул оттуда). События календаря
   теперь видны и на экране «План» за выбранный период.

## Этап 8. Журнал, здоровье на новых iOS, кнопка ассистента — ◐ ⚠

- ☑ **Логирование.** Раньше приложение не писало о себе ничего. Теперь пишет в
  системный журнал по категориям: источники, состояние, план, напоминания,
  модель, питание. Сырые данные здоровья и ключ модели в журнал не идут.
- ☑ **Экран «Диагностика»** в профиле: читает собственный журнал приложения и
  отдаёт его кнопкой «Отправить». Нужен, когда проблема ловится вдали от Mac.
- ☑ **Чтение HealthKit переделано.** Было: девять запросов строго по очереди,
  без ограничения по времени. Один зависший запрос оставлял все следующие в
  состоянии «загружается» навсегда. Стало: параллельно, с пределом ожидания
  восемь секунд на запрос и записью результата в журнал.
- ☑ **Целевая версия iOS понижена с 26.5 до 18.0.** Двадцать шесть с половиной
  стояло с создания проекта и означало, что приложение ставится только на
  самые новые телефоны.
- ☑ **Кнопка «Задать вопрос Linea»** вместо «Что тебе нужно?», со стеклом
  Liquid Glass на iOS 26 и прежним видом на старых системах.
- ☐ Причина, по которой данные не подтягивались на iOS 27, пока не найдена.
  Воспроизвести удалённо нельзя; журнал добавлен именно для этого.

## Этап 9. Итог дня голосом и память Linea — ◐ ⚠

Ветка `feature/voice-check-in` (поверх `feature/customer-feedback-round-1`).
Описание для заказчика — `Docs/check-in.md`, проверка — `Docs/testing-round-2.md`.

- ☑ **Итог дня.** Вечером на «Сегодня» — «Как прошёл день?». Рассказ голосом
  до пяти минут или текстом, разбор, экран «Проверь», сохранение: сделанное
  закрывается, незакрытое переезжает на завтра, пишется дневник. Кнопка
  «Рассказать» и в вечернем уведомлении.
- ☑ **Распознавание русской речи на телефоне.** Своя модель GigaAM-v3 (Сбер,
  MIT) через sherpa-onnx: без сети и без сторонних сервисов, со знаками
  препинания, любой iPhone с iOS 18. Встроена в приложение (233 МБ, ADR-022):
  её кладёт сборка, человеку ничего не нужно скачивать. Проверена на Linux
  тем же движком: живая сбивчивая речь распознана без ошибок. Если модель не
  справилась — системная диктовка iPhone. Голос удаляется сразу после
  распознавания.
- ☑ **Итог дня целиком на телефоне** (ADR-021). Облачное распознавание xAI и
  разбор Grok проверили вживую и 24.09 убрали: голос распознаёт GigaAM,
  рассказ разбирают правила, наружу не уходит ничего, токенов итог не тратит.
  Запрос и проверка ответа для языковой модели остались в ядре — на случай
  модели на самом телефоне (вопрос 34).
- ☑ **Ядро контекста пользователя** по образцу OpenClaw, внутри приложения:
  факты о человеке, дневник с выжимками, поиск по дневнику, консолидация,
  бюджет 900 токенов на запрос. «Запомни, что…» в рассказе и в чате. Экран
  «Профиль» → «Память».
- ☑ **Калибровка по факту.** Ёмкость дня учится на доле сделанного плана из
  итога, а не только на трёх кнопках.
- ☑ **Разбор правилами на живой речи.** Действие без названия относится к
  последней задаче предложения, отрицание — только перед глаголом
  завершения, «был созвон» — «сделано». Закреплено тестом на рассказе,
  распознанном GigaAM.
- ☑ 240 тестов ядра зелёные в Docker и на Apple-тулчейне; сборка iOS с
  sherpa-onnx на CI зелёная.
- ◐ Проверка на телефоне по `Docs/testing-round-2.md`. Первый раунд 24.09:
  память пишется верно; вылет при повторном открытии итога дня, запись в
  фоне, «5:01» на таймере, долгий разбор без сети и копии задач по двойному
  «Готово» исправлены. Нужен повторный прогон (проверки 7, 8, 12); скорость
  модели на iPhone ещё не измерена.

## v1.1 (после приёмки)

- Перенос
  `HealthKitManager.swift` в `Linea/Data/HealthKit` (sed по строкам 10, 15,
  42, 128 pbxproj); `BGTaskScheduler` для уточнения нуджей; on-device
  подсказка «задача относится к цели X»; облачный объяснитель через
  бэкенд-прокси и `POST /v1/explain` в `API/openapi.yaml`.

## Журнал

- **2026-09-25.** Модель распознавания встроена в приложение (ADR-022).
  Кнопки «Скачать» больше нет: шаг сборки «Speech model» один раз на машину
  скачивает закреплённую GigaAM-v3, сверяет по SHA-256 и кладёт в
  `Linea.app/GigaAM`. В git модели нет — GitHub не берёт файлы больше 100 МБ.
  Проверено: скрипт на Linux (загрузка, кэш, подмена файла), сборка на
  macOS-раннере положила модель в приложение, CI кэширует её между прогонами.
  Приложение стало тяжелее на 233 МБ; копия, скачанная в первом раунде,
  удаляется при первом запуске. Вопросы 32 и 33 закрыты.

- **2026-09-24, вечер.** Итог дня — целиком на телефоне (ADR-021). Облачное
  распознавание xAI, разбор Grok и переключатель «Итог дня через Grok»
  удалены. Голос распознаёт GigaAM, пока она не скачана — диктовка iPhone;
  рассказ разбирают правила. Итогу дня больше не нужен ключ, он одинаково
  работает без сети и не тратит токенов. Grok остался только в чате по
  отдельному согласию. Вопросы 26 и 27 закрыты, добавлен вопрос 34 — нужна ли
  языковая модель на телефоне для синонимов.

- **2026-09-24.** Первый прогон итога дня на iPhone. Память пишется верно.
  Нашлось пять проблем, все исправлены:
  - вылет при повторном открытии итога дня после «Закрыть» без сохранения, в
    том числе в авиарежиме. Экран проверки брал черновик через
    `Binding($store.draft)`, а такая привязка падает, когда черновик
    обнуляется. Вторая причина: распознавание и разбор продолжались после
    закрытия экрана и переписывали новый рассказ. Теперь это одна отменяемая
    задача, поздний ответ отбрасывается, модель на телефоне распознаёт записи
    по одной;
  - запись шла после сворачивания приложения. Теперь она останавливается
    сразу, а сказанное распознаётся после возвращения;
  - таймер доходил до «5:01». Пять минут теперь отсчитывает само приложение;
  - без сети «Разбираю…» шло дольше. Точную причину без журнала не
    установить; теперь без сети облако не вызывается вовсе (`NWPathMonitor`),
    а «Диагностика» пишет, сколько секунд занял каждый шаг;
  - двойное «Готово» создавало копии задач и целей. Кнопка срабатывает один
    раз, id новой задачи выбирается один раз.

  Заодно найден и закрыт тестом вылет разбора на огромных числах в рассказе
  или в ответе модели.

- **2026-09-23, вечер.** Заказчик просит обойтись без сторонних сервисов.
  Разобрали локальные варианты для русского. Google на iPhone без сети не
  распознаёт (только Android), новая модель Apple русского не знает, WhisperKit
  тяжёлый (630 МБ, iPhone 13+, минута подготовки после обновлений iOS).
  Выбрана GigaAM-v3 от Сбера через sherpa-onnx. Проверили на Linux тем же
  движком: 39 секунд сбивчивой речи распознаны за 2 секунды слово в слово,
  со знаками препинания. Модель скачивается по кнопке и сверяется по SHA-256.
  Заодно подтянули разбор правилами на живой речи: теперь без облака
  (GigaAM + правила) итог дня разбирается правильно.

- **2026-09-23.** Итог дня голосом и память — этап 9. Разобрали OpenClaw:
  встроить нельзя (Node.js-сервер, телефонные приложения у них — тонкие
  клиенты), его устройство памяти повторено на Swift. Выяснили, что новая
  модель распознавания Apple русского не знает ни в iOS 26, ни в 27, а у xAI с
  апреля есть распознавание на том же ключе. Проверили вживую: синтезированная
  русская речь распознана слово в слово за 1,2–1,4 с, а подсказки-названия
  задач исправили «линия» → «Linea». Сравнили модели разбора на чистом и
  сбивчивом рассказе: grok-4.6 — 32 с, grok-4.3 — 8–12 с, обе без ошибок;
  быстрая модель без рассуждения ошибалась и придумывала факты в память.
  Выбрана grok-4.3. Ядро: +49 тестов, всего 229. Первая сборка на Mac упала
  на старом выражении в `SleepAnalyzer`: компилятор перестал укладываться в
  лимит проверки типов, когда в модуле появились новые типы. Выражение
  разбито на шаги.

- **2026-09-09.** Получен бриф. Изучен репозиторий: SwiftUI-приложение с
  SwiftData (задачи/цели), read-only HealthKit «за сегодня», без тестов и
  без Intelligence. Проверено: чистое Swift-ядро собирается и тестируется в
  Docker (Swift 6.3.3) с настройками, идентичными Xcode 26.6.
- **2026-09-10.** Проведён разбор архитектуры (четыре независимых
  проработки + критика брифа + судейство + синтез). Решения записаны в
  `Docs/decisions.md`. Реализованы этапы 0–1, спецификация
  `Docs/intelligence.md`, рецепт `Docs/connectors.md`.
- **2026-09-10.** Приложение впервые собралось: iOS-таргет и тесты ядра
  зелёные на macOS-раннере GitHub Actions. Причина падений — изоляция
  акторов у протоколов репозиториев; воспроизведена локально в Docker и
  исправлена. Дальше — запуск на реальном iPhone.
- **2026-09-10.** Ядро закончено: Decision Engine (scoring, свободные окна,
  расстановка блоков, ограничение нагрузки, пересборка), Nudge Engine,
  объяснения с golden-строками брифа, валидатор текста от модели, Feedback
  Engine, коннектор питания, use case'ы и сквозной тест wow-сценария.
  153 теста в Docker. Собран Apple-слой: экраны Today, Health, Nutrition,
  Profile, хранилище Intelligence, уведомления с кнопками. Осталось: сборка на
  Mac по чек-листу и on-device модель.
- **2026-09-10.** Готов State Engine и его тесты (55 тестов ядра зелёные в
  Docker). Опорные числа wow-сценария: energy 0.396, сон 0.674, восстановление
  0.04, вердикт «снизить нагрузку». Написан Apple-слой: персистентность
  документами, чтение истории HealthKit, композиционный корень, редакторы
  задачи/цели/питания/профиля. Всё, что импортирует SwiftUI, SwiftData или
  HealthKit, ждёт сборки на Mac.
